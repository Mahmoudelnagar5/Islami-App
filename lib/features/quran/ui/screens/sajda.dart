import 'package:flutter/material.dart';

class SajdaScreen extends StatelessWidget {
  const SajdaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Sajda Screen'),
    );
  }
}
